package SegundaEvaluacion;


/**
 * Write a description of class Pelota here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Pelota
{
   
    //solicita tamaño de tablero en el que se movera una pelota simbolizada por una x
    //el programa colocara en un inicio la posicion de la x en una posicion aleatoria y a continuacion nos solicitara
    //las cordenadas en las que creemos que esta la pelota
    //se pregunta numero de jugadores y se respetaran los turnos
    //si la pelota no estuviese en esa posicion se anotara un fallo para ese jugador (diferentes jugadores) y se cambiara de posicion
    //a la pelota en un rango de dos posiciones a la derecha o a la izquierda si fuera posible
    //el juego terminara cuando algun jugador asi lo decida o cuando se llegue a 5 jugadas, se mostrara el contador de 
    //cada jugador
}
